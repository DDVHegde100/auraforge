# auraforge

my version of luminar neo but free

local aesthetic photo studio. scene analysis, ai enhance, sky masks, 40 grades, 20 signatures — runs on your machine.

```bash
chmod +x dev.sh
./dev.sh
```

then open http://127.0.0.1:5173

| route | purpose |
|-------|---------|
| `/` | editor |
| `/showcase` | before/after look pairs |
| `/research` | algorithm notes summary |

see `PLAN_100.md` for the full roadmap (100 commits). v0.1.0 MVP is complete.
