# v0.1.0 ship checklist

manual pass before tagging `v0.1.0`:

- [ ] clone repo fresh (not nested under a6000)
- [ ] `./dev.sh` starts api :8787 and web :5173
- [ ] open a local portrait / food / landscape image
- [ ] AI enhance slider changes preview perceptually
- [ ] pick any of 40 grades + any of 20 signatures
- [ ] before/after scrubber works
- [ ] export JPEG and TIFF downloads
- [ ] `/showcase` route loads
- [ ] `/research` route loads
- [ ] batch folder mode processes a local directory
- [ ] `pytest tests/engine -q` passes
- [ ] no telemetry / cloud required

```bash
git tag v0.1.0
git push origin v0.1.0
```
